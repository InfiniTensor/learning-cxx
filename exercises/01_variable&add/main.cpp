#include "../exercise.h"
#include <iostream>  // 确保包含iostream头文件

int main(int argc, char **argv) {
    // 补全变量定义
    int x = 5;  // 示例值，可以是任何整数
    
    std::cout << x << " + " << x << " = " << x + x << std::endl;
    return 0;
}
